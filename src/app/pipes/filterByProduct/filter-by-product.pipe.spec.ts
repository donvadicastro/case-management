import { FilterByProjectPipe } from './filter-by-product.pipe';

describe('FilterByProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByProjectPipe();
    expect(pipe).toBeTruthy();
  });
});
