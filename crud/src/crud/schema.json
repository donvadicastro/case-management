{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsCRUD",
  "title": "CRUD Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the entity.",
      "type": "string"
    }
  },
  "required": [
    "name"
  ]
}
